# 🔐 دليل إعداد Firebase للإنتاج

## 📋 الخطوات المطلوبة

### 1️⃣ تفعيل Firebase Authentication

1. افتح **Firebase Console**: https://console.firebase.google.com
2. اختر المشروع: **sara-a3a18**
3. من القائمة الجانبية، اختر **Authentication**
4. اضغط **Get Started**
5. فعّل **Email/Password** من تبويب **Sign-in method**
6. أضف مستخدم من تبويب **Users**:
   - اضغط **Add user**
   - أدخل البريد الإلكتروني وكلمة المرور
   - احفظ بيانات الدخول في مكان آمن

### 2️⃣ إعداد Firestore Database

1. من القائمة الجانبية، اختر **Firestore Database**
2. اضغط **Create database**
3. اختر **Start in production mode**
4. اختر الموقع: **us-central** أو أقرب منطقة
5. اضغط **Enable**

### 3️⃣ نسخ قواعد Firestore Security Rules

انسخ القواعد التالية والصقها في **Firestore Rules**:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // قواعد مجموعة قصص النجاح
    match /testimonials/{document=**} {
      // السماح بالقراءة للجميع
      allow read: if true;
      
      // السماح بالكتابة والتعديل والحذف للمستخدمين المصادقين فقط
      allow create, update, delete: if request.auth != null;
    }
    
    // قواعد مجموعة العملاء
    match /clients/{document=**} {
      // السماح بالقراءة للجميع
      allow read: if true;
      
      // السماح بالكتابة والتعديل والحذف للمستخدمين المصادقين فقط
      allow create, update, delete: if request.auth != null;
    }
    
    // منع الوصول لأي مجموعات أخرى
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

### 4️⃣ نشر القواعد

1. بعد نسخ القواعد أعلاه
2. اضغط **Publish** في أعلى الصفحة
3. انتظر رسالة التأكيد

---

## 🎯 شرح القواعد

### القراءة (Read)
```javascript
allow read: if true;
```
- ✅ يسمح لأي زائر بقراءة البيانات
- ✅ ضروري لعرض الصور على الموقع الرئيسي

### الكتابة (Write)
```javascript
allow create, update, delete: if request.auth != null;
```
- 🔒 يسمح فقط للمستخدمين المصادقين
- 🔒 يحمي من التعديلات غير المصرح بها
- 🔒 الأمان الكامل للبيانات

---

## 🚀 الاستخدام

### الدخول إلى لوحة التحكم
افتح الملف: `admin-control.html`

### بيانات الدخول
استخدم البريد وكلمة المرور التي أنشأتها في خطوة Authentication

### المميزات المتاحة
- ✅ إضافة/تعديل/حذف صور قصص النجاح
- ✅ إضافة/تعديل/حذف شعارات العملاء
- ✅ محول تلقائي لروابط Google Drive
- ✅ حماية كاملة بالمصادقة

---

## 🔍 حل مشكلة "Missing or insufficient permissions"

### السبب
القواعد الافتراضية في Firestore تمنع جميع العمليات

### الحل
1. ✅ تأكد من تفعيل Firestore Database
2. ✅ انسخ القواعد أعلاه بالضبط
3. ✅ اضغط Publish
4. ✅ انتظر دقيقة واحدة للتحديث
5. ✅ حدّث صفحة المتصفح

---

## 📱 ملفات النظام

### 1. `admin-control.html`
لوحة التحكم الرئيسية مع:
- تسجيل الدخول الآمن
- إدارة قصص النجاح
- إدارة شعارات العملاء

### 2. `firebase-config.js`
إعدادات Firebase + Authentication

### 3. `testimonials-loader.js`
تحميل ديناميكي لقصص النجاح في الموقع

### 4. `clients-loader.js`
تحميل ديناميكي لشعارات العملاء في الموقع

---

## 🛡️ نصائح الأمان

### ✅ افعل
- استخدم كلمة مرور قوية (12+ حرف)
- لا تشارك بيانات الدخول
- سجل الخروج بعد الانتهاء
- راجع السجلات في Firebase Console

### ❌ لا تفعل
- لا تحذف قواعد الأمان
- لا تضع `allow write: if true;`
- لا تشارك ملف `firebase-config.js` علناً (رغم أنه آمن مع القواعد)

---

## 📊 مراقبة الاستخدام

### Firebase Console > Usage
- عدد القراءات اليومية
- عدد الكتابات اليومية
- حجم التخزين المستخدم

### الحدود المجانية (Spark Plan)
- ✅ 50,000 قراءة/يوم
- ✅ 20,000 كتابة/يوم
- ✅ 1 GB تخزين

---

## 🆘 استكشاف الأخطاء

### الخطأ: "Missing or insufficient permissions"
**الحل**: انسخ القواعد أعلاه واضغط Publish

### الخطأ: "auth/invalid-credential"
**الحل**: تأكد من صحة البريد وكلمة المرور

### الخطأ: "Firebase: Error (auth/configuration-not-found)"
**الحل**: فعّل Email/Password في Authentication

### الصور لا تظهر في الموقع
**الحل**: 
1. تأكد من نشر القواعد
2. افتح Console (F12) وابحث عن أخطاء
3. تأكد من صحة روابط Google Drive

---

## 📞 الدعم الفني

### Firebase Documentation
https://firebase.google.com/docs/firestore/security/get-started

### Firebase Console
https://console.firebase.google.com/project/sara-a3a18

---

**آخر تحديث**: نوفمبر 2025
**الحالة**: جاهز للإنتاج ✅
